# generated files
target/
generated/

# dependencies
Dependencies.toml

# config files
Config.toml
# the config files used for testing, Uncomment the following line if you want to commit the test config files
#!**/tests/Config.toml

# --- Hoolulu agent ---
# local runtime state (database, logs, backups) must never be committed
data/
logs/
backups/
*.db
*.db-wal
*.db-shm

# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# --- ecosystem hub runtime state ---
builds/
deployed/
packages/
.env
hoolulu-ecosystem-hub/core/event-log/loops/*.jsonl

# skill exports written by `markus skill export` into the working directory
*-claude/
*-openclaw/
*-soul/
*-mcp-server/
*-agentscope/
*-markus/
