"""Hoolulu Ecosystem Hub — the runtime that drives ``hoolulu-ecosystem-hub/``.

Importable Python lives here; the hyphenated tree next door holds the data the
runtime reads and writes (contracts, templates, event log, ops scripts).

    from hub import config
    from hub.reasoning import productizer
"""

__version__ = "0.1.0"
