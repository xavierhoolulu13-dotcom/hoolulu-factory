# {{NAME}}

{{ONE_LINER}}

Generated by the Hoolulu Factory from `{{TEMPLATE}}`.

## Run it

Open `index.html`, or serve the folder:

```bash
python3 -m http.server 8000
```

## What is in here

| File | What it is |
| --- | --- |
| `index.html` | hero, problem, features, pricing, FAQ, closing call to action |
| `style.css` | the theme — accent colour and radius live in `:root` |
| `app.js` | accordion behaviour, year stamp, tap-to-copy |

## Before it goes live

1. Replace the placeholder phone number and contact link.
2. Swap the proof block for real reviews and photos — nothing else converts.
3. Add `LocalBusiness` JSON-LD in `<head>` so Google shows the right hours.
4. Delete this README.

The shape is deliberately one page: a visitor decides in three seconds, and
every extra section costs you that.
