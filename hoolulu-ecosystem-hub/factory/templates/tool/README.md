# {{NAME}}

{{ONE_LINER}}

Generated by the Hoolulu Factory from `{{TEMPLATE}}`.

## Run it

Open `index.html`, or serve the folder:

```bash
python3 -m http.server 8000
```

## What is in here

| File | What it is |
| --- | --- |
| `index.html` | one form, one list, one total |
| `style.css` | theme — accent colour lives in `:root` |
| `app.js` | add / remove / total / export, all in the browser |

Entries are stored in `localStorage` under `{{SLUG}}-entries`. Nothing leaves
the device. Use **Export CSV** before clearing your cache.

## Make it yours

- Field labels are the two `<label>` elements in `index.html`.
- The export filename uses the slug.
- Add a calculated column (tax, miles, rate) in `render()` in `app.js`.
